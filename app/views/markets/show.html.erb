<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "MobileMoneyEcosystem" %></title>
  <%= csrf_meta_tags %>

  <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
  <!--[if lt IE 9]>
  <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
  <![endif]-->
  <link href='http://fonts.googleapis.com/css?family=PT+Sans:400,700' rel='stylesheet' type='text/css'><link href='http://fonts.googleapis.com/css?family=PT+Sans:400,700' rel='stylesheet' type='text/css'>
  <%= stylesheet_link_tag "application", :media => "all" %>

  <!-- For third-generation iPad with high-resolution Retina display: -->
  <!-- Size should be 144 x 144 pixels -->
  <%= favicon_link_tag 'images/apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>

  <!-- For iPhone with high-resolution Retina display: -->
  <!-- Size should be 114 x 114 pixels -->
  <%= favicon_link_tag 'images/apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>

  <!-- For first- and second-generation iPad: -->
  <!-- Size should be 72 x 72 pixels -->
  <%= favicon_link_tag 'images/apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>

  <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
  <!-- Size should be 57 x 57 pixels -->
  <%= favicon_link_tag 'images/apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>

  <!-- For all other devices -->
  <!-- Size should be 32 x 32 pixels -->
  <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
</head>
<body>

<div id="market-proudcts-main-container">
  <!--<div class="row">
    <div class="col-offset-3 col-span-6 tcenter">
      <%# link_to image_tag("logo-square.png",{ :alt => 'logo', :id => '', :class => 'market-logo'}), :root %>
      <span class="market-name"><%= @merchant_name %></span>
    </div>
  </div>-->
  <div>
    <div class="col-offset-3 col-span-6">
      <%= form_tag products_path, :method => :get, :id=>"product-search-form", :style=>"margin:0 -8px;",  :remote => true do%>
        <%= hidden_field_tag @merchant_id %>
        <div class="span search-bar">
          <input id="search" name="q" type="text" class="wsearch" placeholder="Search for products by name and category"
                 value="<%= @query %>" data-provide="typeahead" autocomplete="on" data-source='<%= all_categories %>'>
          <i class="search-reset glyphicon-remove glyphicon"> </i>
          <button class="icon-button" type="submit"><i class="glyphicon-search glyphicon"> </i></button>
        </div>
      <%end%>
    </div>
  </div>
  <div class="row">
    <div class="col-span-12">
      <ul id="market-products-container">
        <%= render "products", products: @products %>
      </ul>
    </div>
  </div>
</div>




<!-- Javascripts
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->
<%= javascript_include_tag "application" %>

</body>
</html>
